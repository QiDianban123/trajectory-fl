# S2-D-01：本地训练适配与状态兼容

- 阶段：S2（D5—D6）
- 角色：D，联邦学习开发
- 分支：`feature/s2-d-client-training-adapter`
- 主评审：A；协作：C
- 关联：F5、AT-03

## 目标

把共享 Trainer 的 FitResult 转换为未来 ClientUpdate 所需信息，不实现 FedAvg 或轮次循环。

## D5

- 生产代码：实现全局初始 state 复制、state ID/hash 和本地训练 adapter。
- 测试：state key/shape/dtype、深拷贝和不可变基线。
- 建议提交：`feat(federated): add local trainer adapter [S2-D-01][F5]`。

## D6

- 联调：将 C 的 checkpoint/FitResult 映射为 state、sample_count 和 stats。
- 测试：训练后 state 与冻结 ClientUpdate、AggregationRequest 兼容。
- 建议提交：`test(federated): verify trainer state adapter [S2-D-01][AT-03]`。

## 交付与验收

- 不复制优化循环；本地训练始终委托共享 Trainer。
- 状态版本、有效样本数和训练统计可追踪。
- 与 C/A 的集中式流程无行为冲突。
