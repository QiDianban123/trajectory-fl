# S4-G-01：正式运行矩阵、结果索引与离线演示

- 阶段：S4（D9—D10）
- 角色：G，实验运行与复现开发
- 分支：`feature/s4-g-reproduction-demo`
- 主评审：D；第二评审：A
- 关联：F6、F7、M2、AT-06—AT-08

## 目标

执行和监控正式三模式矩阵，编码实现结果索引、复现检查与无需网络的演示入口。

## D9

- 生产/运行：实现批量 seed/mode 调度、断点继续、失败重试上限和运行清单。
- 测试：重复 run、失败恢复、控制变量不一致和缺失产物。
- 建议提交：`feat(experiments): run reproducible matrix [S4-G-01][F6]`。

## D10

- 生产代码：实现 `compare`/结果索引、复现实验脚本和离线演示包构建器。
- 系统测试：fixture 生成完整索引/演示；缺失结果明确失败且不打包 raw 数据/checkpoint。
- 建议提交：`feat(experiments): build offline result package [S4-G-01][AT-08]`。

## 交付与验收

- 每个正式结果可追到 code SHA、config、seed、split、manifest 和原始 ResultRecord。
- 演示入口不重新计算指标，不依赖外部网络，能展示三模式结果与失败状态。
- G 的生产代码、单元/集成/系统测试和两日提交记录齐全；D/A 双评审通过。
