# S4-B-01：数据版本与复现审计

- 阶段：S4（D9—D10）
- 角色：B，数据工程开发
- 分支：`feature/s4-b-data-audit`
- 主评审：F；协作：G
- 关联：F1、F4、M2、AT-01、AT-04、AT-08

## 目标

冻结正式实验的数据版本、split 和 partition，提供可审计的数据复现证据。

## D9

- 生产代码：实现 manifest 一致性检查、文件 checksum 和跨模式 split/partition guard。
- 测试：数据被修改、manifest 缺失、错误 split_id 和跨模式不一致。
- 建议提交：`feat(data): add experiment data audit [S4-B-01][M2]`。

## D10

- 文档/验收：完成数据准备、许可、字段、统计、scaler 和复现说明。
- 回归：支持 AT-01/AT-04/AT-08 与 G 的运行索引核验。
- 建议提交：`docs(data): finalize reproduction guide [S4-B-01][AT-08]`。

## 交付与验收

- 每个正式 run 可追到数据版本、checksum、split_id、partition 和 scaler。
- 相同 manifest 可重建相同样本数；不存在泄漏或静默数据变化。
- 只修复阻断问题，不改变已冻结实验数据。
