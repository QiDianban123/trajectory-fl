# S2-B-01：DataLoader、缓存与数据性能

- 阶段：S2（D5—D6）
- 角色：B，数据工程开发
- 分支：`feature/s2-b-dataloader-cache`
- 主评审：F；协作：C
- 关联：F1、F3、AT-03

## 目标

为集中式训练提供稳定、可复现且不泄漏的 processed 数据读取和 batch 性能。

## D5

- 生产代码：完成 processed dataset reader、缓存键和 DataLoader 参数接入。
- 测试：缓存失效、版本不匹配、空 split、batch 数量和顺序。
- 建议提交：`feat(data): add processed dataset cache [S2-B-01][F1]`。

## D6

- 生产/联调：接入 inverse-transform hook，核验 train/val/test 数量和数据版本。
- 测试/记录：小样本读取性能、固定 seed 顺序与 C Trainer 联调。
- 建议提交：`test(data): verify deterministic loaders [S2-B-01][AT-03]`。

## 交付与验收

- DataLoader 输出与 `TrajectoryBatch` shape/dtype 一致。
- 缓存由数据版本、split_id 和配置共同标识；旧缓存不会误用。
- 相关测试、Ruff 和集中式 smoke 通过。
