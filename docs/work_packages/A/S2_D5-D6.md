# S2-A-01：集中式训练入口与端到端集成

- 阶段：S2（D5—D6）
- 角色：A，项目负责人/架构与集成开发
- 分支：`feature/s2-a-centralized-entry`
- 主评审：D；协作：C、E、G
- 关联：F3、F6、M6、AT-03

## 目标

接通 `train --mode centralized`，用共享 Trainer 完成一条命令的集中式小样本闭环；不复制 C 的训练循环。

## D5

- 生产代码：实现模式解析、模型/Trainer 工厂接线、RunPaths 和统一初始化入口。
- 测试：未知模式、配置不一致、输出目录冲突。
- 建议提交：`feat(cli): add centralized train entry [S2-A-01][F6]`。

## D6

- 集成：串联数据、模型、评价、checkpoint、结果与 G 的 runner。
- 测试/文档：增加一条命令 smoke，更新 README 和 MS3 记录。
- 建议提交：`test(integration): verify centralized pipeline [S2-A-01][AT-03]`。

## 交付与验收

- 读取→训练→验证→评价→出图→保存可一次执行。
- config、metadata、log、checkpoint、metrics 和 figures 完整且不覆盖旧 run。
- 小样本损失下降；Ruff、pytest、配置与 CLI 冒烟通过。
