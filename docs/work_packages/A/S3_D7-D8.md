# S3-A-01：三模式统一入口与公平性守卫

- 阶段：S3（D7—D8）
- 角色：A，项目负责人/架构与集成开发
- 分支：`feature/s3-a-unified-mode-cli`
- 主评审：D；协作：C、G
- 关联：F5、F6、AT-05、AT-06

## 目标

统一 Centralized、Local-only、Federated 的 CLI、配置、初始化、输出目录和错误语义。

## D7

- 生产代码：实现模式注册与分发；校验 split_id、model config、seed、initial-state ID 一致。
- 测试：模式缺失、控制变量不一致、非法联邦配置拒绝。
- 建议提交：`feat(cli): register three training modes [S3-A-01][F6]`。

## D8

- 集成：接入 D/G 的 Local-only/Federated runner 和 E 的统一结果记录。
- 测试/文档：三模式 smoke、错误码和产物完整性；记录 MS4 评审。
- 建议提交：`test(system): smoke three mode dispatch [S3-A-01][AT-06]`。

## 交付与验收

- 三模式使用同一 split、模型、seed、初始权重、指标和结果 schema。
- 任一模式失败必须返回非零并保留失败记录。
- CLI 只负责编排；训练/FedAvg 逻辑分别留在 C/D 模块。
