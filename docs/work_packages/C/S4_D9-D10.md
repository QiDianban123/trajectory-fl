# S4-C-01：最终模型配置、推理与结果解释

- 阶段：S4（D9—D10）
- 角色：C，模型与训练开发
- 分支：`feature/s4-c-inference-finalize`
- 主评审：E；协作：A、D
- 关联：F3、F6、AT-03、AT-06

## 目标

冻结最小可复现模型参数，监控正式运行质量并完成推理/检查点验收。

## D9

- 配置/代码：冻结 hidden size、层数、学习率、epoch/local epochs 和梯度裁剪。
- 运行诊断：监控 loss、梯度、NaN/Inf 和过拟合风险；只修复阻断问题。
- 建议提交：`chore(model): freeze v1 hyperparameters [S4-C-01][F3]`。

## D10

- 验收：重复加载最佳 checkpoint，核对预测和指标输入；补足错误提示。
- 文档：解释收敛、已知限制和 Centralized/Local/Federated 差异。
- 建议提交：`test(model): verify final inference restore [S4-C-01][AT-03]`。

## 交付与验收

- 正式配置进入版本控制；运行不依赖代码硬编码超参数。
- checkpoint 可恢复且预测一致；负面结果如实说明。
- 通过 E/A 评审和最终系统回归。
