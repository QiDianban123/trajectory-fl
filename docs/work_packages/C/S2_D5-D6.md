# S2-C-01：LSTM Seq2Seq 与共享 Trainer

- 阶段：S2（D5—D6）
- 角色：C，模型与训练开发
- 分支：`feature/s2-c-lstm-trainer`
- 主评审：E；第二评审：A
- 关联：F3、AT-03

## 目标

实现轻量 LSTM Encoder-Decoder、MSE、TorchTrainer、推理和 checkpoint 闭环。

## D5

- 生产代码：实现 `lstm_seq2seq.py`、forward、损失、optimizer 和 Trainer batch 循环。
- 测试：输出 shape、梯度非空、无 NaN/Inf、错误 device/dtype。
- 建议提交：`feat(model): implement lstm seq2seq [S2-C-01][F3]`。

## D6

- 生产代码：实现 eval/no_grad、梯度裁剪、checkpoint 保存/恢复和 best epoch。
- 测试：小样本过拟合、checkpoint round-trip 和保存前后预测一致。
- 建议提交：`feat(training): implement torch trainer [S2-C-01][AT-03]`。

## 交付与验收

- `forward([B,T_h,2]) -> [B,T_f,2]`，使用冻结配置和共享 Trainer。
- 小样本 loss 明显下降；checkpoint 恢复后预测一致。
- 模型不读取文件、不创建路径、不计算 ADE/FDE。
