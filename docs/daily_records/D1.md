# D1 启动与需求基线记录

**日期：** 2026-08-31<br>
**里程碑：** M1 前置工作（需求基线，D2 设计评审后通过）

## 已完成

1. 确认 P0 为 F1—F7、M1—M6；P1 保留为后续可选增量，P2 不进入当前周期。
2. 选定 highD 为唯一主数据集，记录数据不可用时的替代评审机制。
3. 建立独立 Git 项目根目录、忽略规则、依赖清单、CLI、Issue 模板、源码和测试目录。
4. 记录模型 I/O、Non-IID、FedAvg、ADE/FDE 的 D2 评审草案。
5. 建立 pytest 发现配置、环境检查和 D1 指标数值测试。

## 关键决策

- 历史和未来轨迹均统一为二维平面坐标；batch 维度置前。
- 数据切分先于滑窗构造；归一化只拟合训练数据；指标在物理坐标计算。
- 默认 5 个模拟 RSU，以道路空间区域形成可解释 Non-IID；FedAvg 以有效训练样本数加权。

## 后续

- D2 开始前运行 README 中的环境检查和 pytest，确认团队使用统一的依赖版本。
- highD 原始数据尚未放入 `data/raw/`，D3 首先运行探查脚本并确认字段映射。

## D2 输入

`requirements.md`、`model_contract.md`、`federated_design.md`、`test_plan.md` 和 Backlog 是 D2 设计评审的输入。D2 的输出必须明确接口、数据格式、默认配置和结果 schema。
