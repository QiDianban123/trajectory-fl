# QA 代码审查自检 · 成员④ D3（S1-D-01 上半程）

**审查对象：** 提交 `5c53dcf`（`feat(data): add rsu partition schema [S1-D-01][F4]`）
**审查人：** 成员④（QA 角色自审；正式评审待 A 主评审 / F 第二评审）
**日期：** 2026-09-04（D3）

> 依据 D2 任务卡 ④-A 确立的 QA 检查项（docs/daily_records/S1_D/D2/02 号文档第二节）执行自审。

## 一、检查项与结论

| 检查项 | 结论 | 说明 |
|---|---|---|
| 接口一致性：不擅自改动 D2 冻结契约 | ✅ | 未改动 adapters/dataset/preprocess/federated；`configs/data.yaml` 为 D3 既定扩展点，仅**新增** `partition:` 段并纳入校验，既有键语义不变 |
| 变更影响同步 | ✅ | 数据配置扩展已在 01 号文档/README 记录，并要求与 B/A 合入 `dev` 时按接口审计解决冲突 |
| 防数据泄漏 | ✅（语义层） | partition 模块仅接受训练分组（输入即已按 split 隔离）；单车辆整属一个客户端、不复制样本；跨 split 泄漏断言归 D4/B/F 集成层 |
| 错误可定位（M6） | ✅ | 非法配置/边界/分组在构造或调用期报 `PartitionError`/`ConfigError`，文案含具体键与期望值（如 `region_edges must contain exactly num_clients + 1 edges (6), got 2`） |
| 数值与边界正确性 | ✅ | 区域区间右开（末区间右闭）语义单一来源 `RegionIndex.region_for_coordinate`；边界值用例覆盖 0/内部边界/末边界/范围外/NaN |
| 确定性/可复现 | ✅ | 分组排序与输入顺序无关；无随机源；ID 宽度规则固定（为 D4 稳定重建奠基） |
| 禁止复制训练循环等架构约束 | ✅ | 本次无训练/联邦代码；数据层未引入模型/联邦依赖（模块零第三方依赖） |
| 测试配套：正常/边界/异常 | ✅ | 19 项 partition 单测 + 2 项配置单测，按 D.md D3 四类异常组织 |
| 质量门禁 | ✅ | py_compile 与纯标准库冒烟通过；pytest/Ruff/validate-config 通过 |
| 无受限文件/个人路径 | ✅ | 仅提交 6 个代码/测试/配置文件；无原始数据、输出、日志、密钥或绝对个人路径 |
| 非目标范围（no-scope） | ✅ | 未实现 D4 分配/合并/manifest、未实现 B 的清洗/切分/滑窗、未改 A 的 CLI |

## 二、需要评审者重点确认的开放点

1. **边界语义**：内部边界坐标归入右侧区域、末区间右闭 —— 是否符合团队对“RSU 空间区域”的口径？（A 判）
2. **默认配置**：`partition.min_samples_per_client: 1`（仅空区域触发合并）与 `region_edges: null`（等宽切分）作为 S1 默认值是否合适？（A/F 判）
3. **manifest/统计口径**：D4 将输出的每客户端“样本数”为分组窗口数之和、车辆数为分组数、空间范围为合并后的区域边界 —— 与 G 的 manifest、E 的结果 schema 口径一致性。（A/F/G 判）

## 三、缺陷/建议登记（本日自审发现）

| 编号 | 级别 | 问题 | 状态 |
|---|---|---|---|
| QA-D3-01 | P3（建议） | `PartitionConfig.from_mapping` 未知键采取严格拒绝；若后续 S3 需加入联邦相关键，需经配置变更评审 | 记录跟踪 |
| QA-D3-02 | P3（建议） | 等宽边界在浮点极小跨度下可能产生非严格递增边界的理论风险；当前域值（米级坐标）不受影响，D4 测试补充退化跨度用例 | D4 处理 |

## 四、评审记录

- MR/评审留痕将在 CodeArts MR 评论与缺陷单中补充（成员⑤ 归档截图）；本地以本文档为过程证据。
- 请求 **A**（主评审）与 **F**（第二评审）对提交 `5c53dcf` 执行正式评审。
