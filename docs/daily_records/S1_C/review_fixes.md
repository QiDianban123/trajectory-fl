# C/D S1 评审缺陷修复记录

- 实际日期：2026-09-05。
- 分支：`feature/s1-c-torch-batching`；评审基线：`9190a39`。
- 修复关联：S1-D-01、F4、AT-04；在当前 C 分支继续完成用户要求的 C/D 评审修复。
- 实际修复与回归测试 Commit SHA：`15ed0972ace9be74fa84770fc42a76485c743b7d`。
- Issue/MR：未创建；本地修复通过，未推送或合入 dev。

## 缺陷与修复

| 级别 | 已复现问题 | 修复行为 | 回归验证 |
|---|---|---|---|
| P1 | 按起点排序后的最后一组终点被误用为全局终点 | 全量计算 `min(x_min)` / `max(x_max)` | 嵌套及重叠轨迹覆盖正确；输入乱序重建一致；显式边界拒绝终点越界但中点在界内的轨迹 |
| P2 | 零有效窗口输入生成样本数为零的客户端，并通过不变式检查 | 分区前拒绝总有效窗口数为零的输入；不变式检查拒绝零样本客户端 | 普通区域、零宽区域、显式边界均拒绝；人工构造零样本客户端也无法通过验收 |
| P2 | YAML 配置门禁放过 NaN/Inf 区域边界 | 数字类型检查后立即检查有限值，报错包含字段索引 | `.nan`、`.inf`、`-.inf` 经 YAML 读取和完整配置 bundle 校验均被拒绝 |

保留 `GroupExtent(sample_count=0)` 对预留车辆的表示能力：只要整体有有效训练窗口，
零窗口分组仍参与唯一归属与统计，不删除或复制分组。若合并到唯一客户端后有效样本数仍低于
`min_samples_per_client`，继续保留该正样本客户端，沿用原有阈值作为合并触发条件的语义。
本次没有改变 C 桥接接口、分区 manifest 字段或依赖。

## 测试证据

- 新增 11 项测试；修复前针对 partition/config 执行得到 **10 failed, 35 passed**，
  证明缺陷场景被捕获；另一个新增用例验证预留零窗口分组的兼容行为。
- `python -m pytest -q`：**128 passed in 10.89s**（原基线 117 项）。
- `python -m ruff check src tests`：All checks passed。
- `python -m src.cli validate-config`：Configuration valid: run=d2-smoke mode=smoke seed=42。
- `git diff --check`：通过。
- 首轮 Ruff 发现一处测试装饰器行宽超限；仅调整换行后重新检查通过。

## 验收边界与交接

三项已复现缺陷已修复并通过回归；这不代表完整 S1 阶段验收或人工评审已完成。
此前审查提到的 train-only 生产接入及 validation/test 误入反例集成证据仍待 B/D 联调补齐。
没有通过修改公共 split 契约或伪造上游输入来声称该集成缺口已经关闭。

请 D/A/F 复核完整轨迹范围、有效样本定义和配置门禁；C 的 B/E 对接及公共接口第二评审仍待完成。
