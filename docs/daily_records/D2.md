# D2 设计与接口评审记录

**日期：** 2026-09-01<br>
**状态：** 进行中（A 的架构与配置基线完成，等待 B—F 逐项确认）

## A 已完成

1. 建立 `docs/design.md`，明确分层依赖、标准样本/批数据、坐标与归一化边界、配置层级、CLI、输出目录和统一结果 schema。
2. 建立 data/model/smoke 三份最小 YAML 配置及加载、校验和跨配置维度一致性检查。
3. 将 `status`、`validate-config`、`prepare-data`、`train`、`compare` 纳入 CLI 契约；未实现命令明确返回退出码 2。
4. 增加配置正常、空文件、防数据泄漏、缺文件和未实现命令测试。

## 验证记录

- 已通过 `pytest`。
- `python -m src.cli status` 与默认 `python -m src.cli validate-config` 执行成功。
- `python -m src.cli train` 按 D2 契约明确提示未实现并返回退出码 2。
- 提交前已执行 `git diff --check` 和暂存文件清单检查。

## 待 B—F 评审确认

| 成员 | 待确认内容 | 当前建议 |
|---|---|---|
| B | highD 字段映射、meta、split/scaler 持久化 | 先按 vehicle ID 切分；train-only 标准化 |
| C | 表示方式、Trainer 返回值、dtype/device | P0 默认绝对位置、float32、batch-first |
| D | ClientUpdate、非浮点 buffer、轮次统计 | 非浮点 buffer 保留全局值 |
| E | Local-only 汇总和图表/结果导出接口 | JSON 为事实源，CSV 由 JSON 记录扁平化 |
| F | run_id、日志、路径、seed、共享夹具 | 唯一运行目录；覆盖 Python/NumPy/PyTorch 随机源 |

任何修改公共 schema 的意见必须记录影响范围，并同步 `docs/design.md`、配置、实现和测试后才能冻结。
