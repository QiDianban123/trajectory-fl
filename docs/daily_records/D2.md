# D2 设计与接口评审记录

**日期：** 2026-09-01<br>
**状态：** 进行中（A 的架构与配置基线完成，等待 B—F 逐项确认）

## A 已完成

1. 建立 `docs/design.md`，明确分层依赖、标准样本/批数据、坐标与归一化边界、配置层级、CLI、输出目录和统一结果 schema。
2. 建立 data/model/smoke 三份最小 YAML 配置及加载、校验和跨配置维度一致性检查。
3. 将 `status`、`validate-config`、`prepare-data`、`train`、`compare` 纳入 CLI 契约；未实现命令明确返回退出码 2。
4. 增加配置正常、空文件、防数据泄漏、缺文件和未实现命令测试。

## B 已完成

1. 冻结 `DatasetAdapter`、`TrajectorySample`、`TrajectoryDataset` 和 `WindowSpec` 的数据层接口与类型约定。
2. 实现并测试“group 先切分、再滑窗”、严格 frame 时间顺序，以及训练集专属 scaler 的防泄漏契约。
3. 补全 highD 坐标单位、切分策略、归一化产物和异常处理配置，并纳入 YAML 校验。
4. 未改动跨模块结果 schema；真实字段映射与 scaler 最终落盘格式保留至 D3。

## C 已完成

1. 冻结 batch-first `float32` 模型 I/O、归一化绝对位置语义、device 责任和可读的 PyTorch 缺失错误。
2. 建立共享 `Trainer`、Centralized 与 Local-only 委托接口，禁止复制训练循环。
3. 冻结统一初始状态、训练/评价统计和 checkpoint 元数据 envelope；未实现 LSTM、优化器或真实训练。
4. 补全模型配置及 YAML 校验，并用 CPU shape/错误路径测试覆盖模型与训练契约。

## D 已完成

1. 冻结 Client、Server、客户端选择、显式失败、AggregationRequest/Result 和 Aggregator 接口。
2. 实现 state_dict key/shape/dtype/有限值、round/global-state、新鲜度及有效样本数校验，未实现 FedAvg 数值计算。
3. 非浮点 buffer 固定为 `preserve_global`，并记录其类型安全与可复现理由。
4. Local-only 在调用共享 Trainer 前深拷贝统一初始状态；实验编排器必须为每个客户端创建新模型实例。

## E 已完成

1. 冻结 ADE/FDE 仅在反归一化后的米制物理坐标中计算，错误单位、shape 和非有限输入均拒绝。
2. 建立轨迹、收敛曲线和三模式对比图接口，统一返回输出路径且不触发训练。
3. 建立 `ResultRecord`、`ResultStore`、JSON/CSV 序列化接口，固定 run、代码版本、seed、split、模式、样本数、ADE/FDE、耗时和产物路径字段。
4. 增加已知数值、单位边界、形状错误、结果字段和图表路径测试；不运行真实实验。

## F 已完成

1. 建立 Python/NumPy/PyTorch 随机源、JSON 运行日志、唯一 run 输出目录和路径逃逸拒绝接口。
2. 扩展 pytest 共享夹具，提供小型轨迹、配置副本和临时输出根目录。
3. 固化 pytest、Ruff 和依赖清单质量门禁；验证记录只保留命令与结果，不包含设备专属信息。
4. 接口审计：数据样本使用 NumPy，训练 batch 使用 Torch；D4 DataLoader 必须完成显式转换。Local-only 的客户端级/宏平均结果字段是 D7—D11 的扩展，当前 `ResultRecord` 仅表示单个可比较结果。

验证基线：`python -m pytest -q` 与 `python -m src.cli validate-config` 已执行；Ruff 已进入依赖清单，需在完整依赖安装后执行 `python -m ruff check src tests`。

## 验证记录

- 已通过 `pytest`。
- `python -m src.cli status` 与默认 `python -m src.cli validate-config` 执行成功。
- `python -m src.cli train` 按 D2 契约明确提示未实现并返回退出码 2。
- 提交前已执行 `git diff --check` 和暂存文件清单检查。

## 待 B—F 评审确认

| 成员 | 待确认内容 | 当前建议 |
|---|---|---|
| B | highD 字段映射、scaler 最终落盘格式 | D3 用真实样例确认；meta/split/scaler 契约已冻结 |
| C | LSTM 的具体网络层与超参数调优 | D5 实现；接口、初始状态和 checkpoint 契约已冻结 |
| D | 客户端采样策略和 FedAvg 数值实现 | D8 实现；接口、失败和非浮点策略已冻结 |
| E | 正式实验汇总与图表定稿 | D11—D13 生成；指标、图表和 JSON/CSV 接口已冻结 |
| F | 日志、路径、seed、共享夹具和质量门禁 | 接口已冻结；Ruff 在完整依赖安装后执行 |

任何修改公共 schema 的意见必须记录影响范围，并同步 `docs/design.md`、配置、实现和测试后才能冻结。
